import React from "react";
import styles from "./profile.module.css";

const Profile = (props) => {
  return (
    <section className={styles.profile}>
      <h1 className={styles.title}>정보</h1>
    </section>
  );
};

export default Profile;
